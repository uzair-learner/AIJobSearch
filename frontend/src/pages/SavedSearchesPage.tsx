export default function SavedSearchesPage() {
  return (
    <section className="page">
      <header className="page-header">
        <div>
          <p className="eyebrow">Saved Searches</p>
          <h1>Saved-search persistence is scaffolded for follow-up implementation.</h1>
        </div>
      </header>
      <section className="panel">Search filters can be stored in the backend `saved_searches` table in the next phase.</section>
    </section>
  );
}
